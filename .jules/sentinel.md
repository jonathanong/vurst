## 2024-05-25 - Prevent SSRF/Open Redirects via Protocol-Relative Backslash Bypasses
**Vulnerability:** URL safety checks (like `is_relative_url`) intended to reject protocol-relative URLs (`//example.com`) only checked for the standard forward slash pattern. This allowed an attacker to bypass the check by using backslash variations (`\\`, `/\`, or `\/`), which parsers or browsers normalize to `//` and treat as protocol-relative URLs.
**Learning:** Browsers and URL parsers often normalize backslashes to forward slashes. Filtering out `//` is insufficient as an attacker can provide `\\attacker.com` to achieve an open redirect or SSRF, which the browser executes as `//attacker.com`. A comprehensive check must account for all combinations of forward and backward slashes at the start of a URL.
**Prevention:** When checking for protocol-relative URLs without incurring full string decoding overhead, use a byte slice pattern match on the first two bytes to check for all slash variations: `matches!(url.as_bytes(), [b'/' | b'\\', b'/' | b'\\', ..])`.
## 2025-02-28 - [SSRF Bypass in Image Proxy Validation]
**Vulnerability:** The `is_relative_url` function checks for protocol-relative URLs (`//`, `\\`, etc.) using a two-character pattern match, but misses URLs starting with a single backslash like `\attacker.com`. Such URLs are classified as relative URLs, which then bypasses checks and might lead to SSRF or open redirect vulnerabilities when the browser normalizes `\` to `/`.
**Learning:** Checking for double slashes/backslashes is insufficient because parsers/browsers normalize single backslashes in certain contexts (like href/src) into absolute URLs on the same protocol, which allows bypass of protocol-relative defenses. A single backslash at the start of a URL should also be treated as potentially dangerous, consistent with how `has_dangerous_prefix` behaves in `packages/markdown/src/markdown_to_html/helpers.rs`.
**Prevention:** In addition to checking for `[b'/' | b'\\', b'/' | b'\\', ..]`, always check if the URL starts with `b'\\'` to prevent normalization bypasses.
## 2025-02-18 - XSS Bypass via HTML Entity Encoded URL Schemes
**Vulnerability:** The HTML sanitizer (`packages/html`) and markdown renderer (`packages/markdown`) validated URL schemes by checking the literal string representation (e.g., matching against `javascript:`). This allowed an attacker to bypass the checks by providing a URL with an HTML entity encoded scheme, such as `javascript&#58;alert(1)`. The sanitizer would not strip the URL because it did not match `javascript:`, but once rendered in an HTML context, the browser would decode the entity and execute the XSS payload.
**Learning:** Browsers decode HTML entities in attributes before validating or executing their contents. Simple string matching on raw, un-escaped HTML attribute strings is insufficient for security validations like checking for dangerous URL schemes.
**Prevention:** Always decode HTML entities from input strings before performing security validation checks (such as URL scheme verification). This ensures the validation logic examines the exact same string that the browser will ultimately process.
## 2026-06-28 - Replaced unsafe String::from_utf8_unchecked with expect\n**Vulnerability:** Unsafe block converting arbitrary filtered bytes to a String without safety guarantees.\n**Learning:** While filtering ASCII bytes theoretically maintains valid UTF-8, manual  code introduces unnecessary risk and maintenance burden when a safe  validates it with negligible overhead.\n**Prevention:** Rely on Rust's standard library safe methods like  for internal invariants rather than resorting to  blocks for micro-optimizations, unless strictly proven necessary by profiling.
## $(date +%Y-%m-%d) - Replaced unsafe String::from_utf8_unchecked with expect
**Vulnerability:** Unsafe block converting arbitrary filtered bytes to a String without safety guarantees.
**Learning:** While filtering ASCII bytes theoretically maintains valid UTF-8, manual `unsafe` code introduces unnecessary risk and maintenance burden when a safe `.expect()` validates it with negligible overhead.
**Prevention:** Rely on Rust's standard library safe methods like `String::from_utf8().expect()` for internal invariants rather than resorting to `unsafe` blocks for micro-optimizations, unless strictly proven necessary by profiling.
